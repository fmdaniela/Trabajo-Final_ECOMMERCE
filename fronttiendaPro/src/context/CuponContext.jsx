import { createContext } from 'react';

const CuponContext = createContext();

export default CuponContext;

//Esto es lo que define el contexto y lo exporta. Nada más.
//CuponContext crea un "canal global" de datos (cupón activo)


